# 股票行情追踪器 (Stock Tracker) 项目报告

作者: Yicheng

 



 

目录
一、项目概述

二、项目整体架构

三、项目模块详述

1. 模块1
2. 模块2

总结



## 一、项目概述

本项目是一个基于 Python 开发的命令行股票行情追踪与分析工具，旨在为用户提供便捷、高效的金融数据查看体验。项目集成了 Baostock（A股）、Yahoo Finance（美股/实时行情）以及 S&P 500 列表等多个数据源，实现了跨市场的股票实时监控。

除了基础的行情查询功能外，本项目还具备以下特色：
*   **自选股管理**：支持模糊搜索添加、本地持久化存储。
*   **AI 智能总结**：接入 OpenAI 兼容接口，根据自选股表现自动生成市场分析日报。
*   **现代化终端 UI**：利用 `Rich` 库构建了美观的 TUI (Text User Interface)，支持彩色动态刷新、分页浏览等功能。

## 二、项目整体架构

项目的代码结构清晰，采用了模块化的分层设计，主要包含以下几个核心部分：

1.  **入口层 (`main.py`)**：程序的启动入口。负责初始化系统组件（数据源、UI、自选股管理器），并维护主事件循环 (Event Loop)，响应用户的菜单操作。
2.  **UI 展示层 (`ui.py`)**：负责所有与用户交互的界面渲染。封装了 `Rich` 库的组件，提供菜单、表格、面板等可视化元素。
3.  **数据层 (`stock_data.py`)**：负责底层数据的获取与清洗。实现了多线程并发请求、数据缓存策略以及多数据源（Baostock/YFinance）的聚合。
4.  **业务逻辑层**：
    *   **自选股管理 (`watchlist.py`)**：处理用户关注股票列表的增删改查及持久化。
    *   **AI 分析器 (`ai_summarizer.py`)**：负责构建提示词并调用 LLM API 生成财经摘要。

### 数据流图简述
用户操作 -> main.py (调度) -> stock_data.py (获取/处理) -> main.py (返回数据) -> ui.py (渲染展示) -> 用户
             |
             v
        watchlist.py (持久化) / ai_summarizer.py (智能分析)

## 三、项目模块详述

本章节将详细介绍系统中的两个核心功能模块：数据处理模块与用户交互模块。

### 1. 数据获取与处理模块 (stock_data.py)

该模块是项目的基石，主要由 `StockDataProvider` 类实现。

*   **多源融合**：
    *   **A股列表**：对接 `Baostock` 接口，支持自动获取最近交易日的全市场代码。
    *   **美股列表**：从 Wikipedia 抓取 S&P 500 成分股，保障了标的规范性。
    *   **实时行情**：使用 `yfinance` 获取实时价格、历史 K 线。
*   **性能优化**：
    *   **并发请求**：使用 `ThreadPoolExecutor` 实现 `get_prices_batch` 方法，大幅提升了批量查询自选股时的响应速度。
    *   **缓存机制**：`ticker_cache` 避免了重复的网络请求，提高了搜索和列表浏览的效率。

### 2. 用户界面与交互模块 (ui.py & main.py)

该模块致力于提供超越传统命令行的用户体验。

*   **界面渲染 (`ui.py`)**：
    *   **Rich 库应用**：大量使用 `Panel` (面板)、`Table` (表格) 和 `Live` (状态加载) 组件。
    *   **视觉反馈**：股票涨跌幅采用红(跌)/绿(涨)自动着色，直观展示市场表现。
    *   **分页系统**：`display_stock_list_page` 实现了全市场浏览时的分页逻辑，支持上一页/下一页/跳转功能。
*   **交互逻辑 (`main.py`)**：
    *   **主循环**：采用 `while True` + 菜单选择的模式，保证程序持续运行。
    *   **输入处理**：封装了用户输入验证，支持模糊搜索（输入 "Apple" 自动关联 AAPL）和快捷代码录入。

### 3. 自选股管理模块 (watchlist.py)

负责用户数据的持久化存储与管理，保障用户配置在程序关闭后不丢失。

*   **数据结构**：管理包含股票代码和市场标识的字典列表。
*   **持久化**：使用 JSON 格式 (`watchlist.json`) 保存用户数据，确保跨平台兼容性和可读性。
*   **去重逻辑**：在添加股票时自动检查重复项，防止列表冗余。

### 4. AI 智能分析模块 (ai_summarizer.py)

利用大语言模型 (LLM) 为金融数据增值。

*   **Prompt 工程**：构建专门的金融分析提示词，将结构化的股票行情数据转换为自然语言的市场简报。
*   **接口封装**：通过 `OpenAI` SDK 兼容多种模型后端（如 GPT-4, DeepSeek 等），通过环境变量配置，灵活度高。

## 四、项目主要界面贴图或者主要实验结果

由于本项目为终端应用程序，以下为核心功能的界面与结果描述：

1.  **自选股列表界面**

<img 自选股列表界面：展示包含代码、名称、最新价、涨跌幅、成交量等列的表格，正收益绿色，负收益红色>

2.  **个股详情**

<img 个股详情界面：上部面板显示价格详情，下部显示最近10日历史行情表格>

3.  **AI 市场总结**

<img AI总结界面：带有紫色边框的面板，显示生成的中文市场分析文本>

*(注：实际运行效果可见于运行截图)*

## 五、总结

本项目通过整合 Python 生态中的优秀开源库（Rich, yfinance, Baostock），成功构建了一个轻量级但功能强大的终端股票追踪工具。

*   **完成度**：实现了从数据获取、本地存储到界面展示、AI 分析的完整闭环。
*   **创新点**：将 Generative AI (LLM) 引入传统终端工具，提供了更智能的数据解读能力。
*   **扩展性**：模块化的代码结构使得未来增加新的数据源（如港股、加密货币）或新的分析功能变得十分容易。

通过本项目的开发，充分验证了 Python 在快速构建实用工具方面的优势，同时也探索了 AI Agent 在金融数据分析场景下的应用潜力。