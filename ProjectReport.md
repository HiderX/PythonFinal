# 股票行情追踪器 (Stock Tracker) 项目报告

作者: Yicheng Kang, Zhufeng Lu


## 目录

一、项目概述

二、项目整体架构

三、项目模块详述

1. 数据获取与处理模块 (stock_data.py)
2. 用户界面与交互模块 (ui.py & main.py)
3. 自选股管理模块 (watchlist.py)
4. 数据持久化模块 (database.py)
5. AI 智能分析模块 (ai_summarizer.py)

四、项目主要界面与功能展示

五、项目总结



## 一、项目概述

本项目是一个基于 Python 开发的命令行股票行情追踪与分析工具，旨在为用户提供便捷、高效的金融数据查看体验。项目集成了 **Akshare**（A股实时行情）、**Yahoo Finance**（美股实时行情）以及 **S&P 500** 成分股列表等多个数据源，实现了跨市场的股票实时监控。

除了基础的行情查询功能外，本项目还具备以下特色：

*   **多市场支持**：同时支持中国 A 股市场和美股市场，覆盖全球主要股票市场。
*   **智能自选股管理**：支持模糊搜索添加（如输入 "Apple"、"万达" 或 "000001"），自动识别市场，本地持久化存储。
*   **高效数据缓存**：采用 SQLite 数据库进行全量数据持久化，每日仅需一次全量更新，后续浏览实现秒级响应。
*   **AI 智能总结**：接入 OpenAI 兼容接口（支持 GPT-4、DeepSeek 等），根据自选股表现或整体市场指数自动生成专业的市场分析日报。
*   **现代化终端 UI**：利用 `Rich` 库构建了美观的 TUI (Text User Interface)，支持彩色动态刷新、分页浏览、涨跌颜色标识等功能。
*   **健壮性设计**：完善的错误处理机制，网络请求降噪，提供友好的用户提示。

## 二、项目整体架构

项目的代码结构清晰，采用了模块化的分层设计，主要包含以下几个核心部分：

1.  **入口层 (`main.py`)**：程序的启动入口。负责初始化系统组件（数据源、UI、自选股管理器），并维护主事件循环 (Event Loop)，响应用户的菜单操作。
2.  **UI 展示层 (`ui.py`)**：负责所有与用户交互的界面渲染。封装了 `Rich` 库的组件，提供菜单、表格、面板等可视化元素。
3.  **数据层 (`stock_data.py`)**：负责底层数据的获取与清洗。实现了多线程并发请求、数据缓存策略以及多数据源（Akshare/YFinance）的聚合。
4.  **数据持久化层 (`database.py`)**：使用 SQLite 数据库管理自选股列表、市场数据缓存和 AI 分析结果，实现高效的数据存储与查询。
5.  **业务逻辑层**：
    *   **自选股管理 (`watchlist.py`)**：处理用户关注股票列表的增删改查，通过数据库实现持久化。
    *   **AI 分析器 (`ai_summarizer.py`)**：负责构建提示词并调用 LLM API 生成财经摘要，支持自选股分析和整体市场分析两种模式。

### 数据流图简述

```
用户操作 
  -> main.py (调度) 
  -> stock_data.py (获取/处理) 
  -> database.py (缓存查询/存储)
  -> main.py (返回数据) 
  -> ui.py (渲染展示) 
  -> 用户

并行路径：
  -> watchlist.py (自选股管理) -> database.py (持久化)
  -> ai_summarizer.py (智能分析) -> OpenAI API
```

## 三、项目模块详述

本章节将详细介绍系统中的核心功能模块，包括数据处理、用户交互、数据持久化和 AI 分析等模块。

### 1. 数据获取与处理模块 (stock_data.py)

该模块是项目的基石，主要由 `StockDataProvider` 类实现，负责从多个数据源获取股票行情数据。

#### 1.1 多源数据融合

*   **A股数据源**：
    *   使用 **Akshare** 库的 `stock_zh_a_spot_em()` 接口获取 A 股实时行情数据
    *   自动获取全市场股票代码、名称、最新价、涨跌幅、成交量等信息
    *   支持上海（.SS）、深圳（.SZ）、北京（.BJ）三大交易所
*   **美股数据源**：
    *   从 Wikipedia 抓取 **S&P 500** 成分股列表，保障标的规范性
    *   使用 **Yahoo Finance** (yfinance) 获取实时价格和历史 K 线数据
    *   支持批量下载，提高数据获取效率
*   **市场指数**：
    *   支持获取上证指数、深证成指、创业板指（A股）
    *   支持获取标普 500、道琼斯、纳斯达克（美股）

#### 1.2 性能优化策略

*   **数据库缓存机制**：
    *   所有市场数据按日期缓存到 SQLite 数据库
    *   每日首次访问时全量更新，后续访问直接从数据库读取
    *   大幅减少网络请求，实现秒级数据加载
*   **并发批量请求**：
    *   使用 `ThreadPoolExecutor` 实现多线程并发请求
    *   `get_prices_batch` 方法支持批量获取多只股票价格
    *   使用 yfinance 的 `download` 方法进行批量下载，提高效率
*   **内存缓存**：
    *   `ticker_cache` 在内存中缓存市场代码列表
    *   避免重复的网络请求，提高搜索和列表浏览效率
*   **智能数据刷新**：
    *   自动检测数据库数据新鲜度（按日期判断）
    *   仅在数据过期时进行网络请求，平衡实时性和性能

#### 1.3 核心方法

*   `get_price(symbol, market)`: 获取单只股票的实时价格信息
*   `get_prices_batch(stocks)`: 批量获取多只股票价格，优先从数据库读取
*   `get_history(symbol, market, period)`: 获取股票历史 K 线数据
*   `get_market_tickers(market)`: 获取指定市场的全量股票列表
*   `search_stocks(keyword)`: 支持模糊搜索股票（按代码或名称）
*   `get_market_indices()`: 获取全球主要市场指数数据

### 2. 用户界面与交互模块 (ui.py & main.py)

该模块致力于提供超越传统命令行的用户体验，通过 Rich 库构建现代化的终端界面。

#### 2.1 界面渲染 (`ui.py`)

*   **Rich 库组件应用**：
    *   `Panel`: 用于菜单、标题和内容面板，提供清晰的视觉层次
    *   `Table`: 用于展示股票列表、自选股、市场指数等表格数据
    *   `Live`: 用于显示动态加载状态，提升用户体验
    *   `Console`: 统一的控制台输出管理，支持彩色文本和格式化
*   **视觉反馈设计**：
    *   股票涨跌幅采用颜色标识：**绿色表示上涨，红色表示下跌**
    *   自动添加正负号标识，直观展示涨跌方向
    *   错误信息使用红色高亮显示
    *   成功操作使用绿色提示
*   **分页浏览系统**：
    *   `display_stock_list_page` 实现全市场浏览的分页逻辑
    *   支持 `n` (下一页)、`p` (上一页)、`j` (跳转指定页)、`q` (返回) 快捷键
    *   显示当前页码、总页数和总股票数量
    *   每页默认显示 20 只股票，可根据需要调整

#### 2.2 交互逻辑 (`main.py`)

*   **主事件循环**：
    *   采用 `while True` + 菜单选择的模式，保证程序持续运行
    *   每次操作后清屏，保持界面整洁
    *   支持 `q` 键退出程序，`Ctrl+C` 优雅退出
*   **菜单功能**：
    *   **[1] 查看自选股**：批量获取并展示自选股列表的实时行情
    *   **[2] 添加自选股**：支持模糊搜索和直接代码输入两种模式
    *   **[3] 删除自选股**：根据股票代码删除自选股
    *   **[4] 个股详情**：查看单只股票的详细信息和近一个月历史 K 线
    *   **[5] 自选股 AI 总结**：基于自选股表现生成 AI 分析报告
    *   **[6] 市场列表**：分页浏览全市场股票，支持 A 股和美股
    *   **[7] 整体市场分析**：查看主要指数和 AI 生成的宏观市场分析
*   **输入处理**：
    *   封装了用户输入验证，提供友好的错误提示
    *   支持模糊搜索（输入 "Apple" 自动关联 AAPL）
    *   支持直接代码输入模式（按 `D` 键进入）
    *   自动处理市场标识（US/CN），支持大小写不敏感

### 3. 自选股管理模块 (watchlist.py)

负责用户自选股列表的管理，提供简洁的接口封装数据库操作。

*   **核心功能**：
    *   `get_watchlist()`: 获取当前自选股列表
    *   `add_stock(symbol, market)`: 添加股票到自选股
    *   `remove_stock(symbol)`: 从自选股中删除股票
*   **设计特点**：
    *   采用薄封装设计，将具体持久化逻辑委托给 `DatabaseManager`
    *   提供简洁的 API，便于主程序调用
    *   自动处理重复添加（数据库层面使用 `INSERT OR IGNORE`）

### 4. 数据持久化模块 (database.py)

使用 SQLite 数据库实现高效的数据存储与管理，是项目性能优化的核心。

#### 4.1 数据库设计

*   **watchlist 表**：存储用户自选股列表
    *   字段：`symbol` (股票代码)、`market` (市场标识)、`added_at` (添加时间)
    *   主键：`(symbol, market)` 组合，防止重复添加
*   **market_tickers 表**：存储市场全量股票数据缓存
    *   字段：`symbol`、`market`、`name`、`price`、`change_percent`、`volume`、`date`、`updated_at`
    *   主键：`(symbol, market, date)` 组合，支持按日期查询
    *   实现每日数据缓存，大幅提升查询性能
*   **market_summary 表**：存储 AI 生成的市场分析报告
    *   字段：`date` (日期)、`summary` (分析文本)、`created_at` (创建时间)
    *   主键：`date`，每日仅生成一次，避免重复调用 API

#### 4.2 核心方法

*   `check_market_data_freshness(market, date)`: 检查指定市场数据是否已缓存
*   `get_market_tickers_list(market, date)`: 获取指定市场的全量股票列表
*   `save_market_tickers_list(market, date, tickers)`: 批量保存市场股票数据
*   `get_stock_data(symbol, market, date)`: 获取单只股票的缓存数据
*   `get_market_summary(date)`: 获取指定日期的市场分析报告
*   `save_market_summary(date, summary)`: 保存市场分析报告

#### 4.3 性能优势

*   使用关系型数据库存储，支持高效的索引查询
*   批量操作使用 `executemany`，提高写入效率
*   按日期分区存储，便于数据管理和清理
*   自动处理并发访问，使用连接池模式

### 5. AI 智能分析模块 (ai_summarizer.py)

利用大语言模型 (LLM) 为金融数据增值，提供专业的市场分析报告。

#### 5.1 功能特点

*   **自选股分析** (`summarize_market`):
    *   基于用户自选股列表的实时行情数据
    *   重点关注涨跌幅度较大的股票
    *   生成整体市场情绪判断
    *   输出简洁的中文分析报告
*   **整体市场分析** (`summarize_overall_market`):
    *   基于全球主要市场指数（上证、深证、创业板、标普 500、道琼斯、纳斯达克）
    *   分别点评中国市场（A股）和美国市场表现
    *   生成宏观市场行情综述
    *   提供趋势分析和投资建议

#### 5.2 技术实现

*   **Prompt 工程**：
    *   构建专门的金融分析提示词模板
    *   将结构化的股票行情数据转换为自然语言描述
    *   设置系统角色为"专业的金融市场分析师"
    *   要求输出纯文本格式，避免 Markdown 标记
*   **接口封装**：
    *   通过 `OpenAI` SDK 调用 LLM API
    *   兼容多种模型后端（GPT-4、GPT-3.5、DeepSeek、ECNU-API 等）
    *   通过环境变量配置 API Key、Base URL 和模型名称
    *   支持自定义温度参数（temperature=0.7）控制输出随机性
    *   限制最大 token 数量，控制成本
*   **错误处理**：
    *   检测 API Key 配置，未配置时提供友好提示
    *   捕获 API 调用异常，返回错误信息
    *   处理空数据情况，避免无效请求

## 四、项目主要界面与功能展示

由于本项目为终端应用程序，以下为核心功能的界面与结果描述：

### 1. 主菜单界面

程序启动后显示主菜单，采用蓝色边框的 Panel 组件，清晰展示所有功能选项：
- [1] 查看自选股行情
- [2] 添加自选股
- [3] 删除自选股
- [4] 查看个股详情
- [5] 生成自选股今日行情总结
- [6] 查看市场列表
- [7] 整体市场行情分析
- [q] 退出

### 2. 自选股列表界面

展示用户关注的所有股票实时行情，采用 Rich Table 组件：
- **表格列**：代码（青色）、名称（洋红色）、市场、最新价、涨跌额、涨跌幅、成交量
- **颜色标识**：上涨显示绿色，下跌显示红色，直观展示市场表现
- **数据来源**：优先从数据库缓存读取，实现秒级加载
- **批量获取**：使用并发请求，同时获取多只股票数据

### 3. 个股详情界面

展示单只股票的详细信息：
- **基本信息面板**：股票名称、代码、最新价、涨跌幅
- **历史行情表格**：显示最近 10 天的 K 线数据
  - 包含日期、开盘价、收盘价、最高价、最低价、成交量
  - 收盘价根据涨跌情况着色（上涨绿色，下跌红色）
  - 数据按日期倒序排列，最新数据在前

### 4. 市场列表浏览界面

分页展示全市场股票列表：
- **分页信息**：显示当前页码、总页数、总股票数量
- **表格展示**：代码、名称、最新价、涨跌幅、成交量
- **导航操作**：
  - `n`: 下一页
  - `p`: 上一页
  - `j`: 跳转到指定页码
  - `q`: 返回主菜单
- **性能优化**：首次访问时从网络获取并缓存，后续访问直接从数据库读取

### 5. AI 市场总结界面

展示 AI 生成的市场分析报告：
- **自选股总结**：基于用户自选股列表生成个性化分析
  - 使用紫色边框的 Panel 组件
  - 重点关注涨跌幅度较大的股票
  - 提供整体市场情绪判断
- **整体市场分析**：
  - 上部显示全球主要市场指数表格（上证、深证、创业板、标普 500、道琼斯、纳斯达克）
  - 下部显示 AI 生成的宏观市场分析报告（黄色边框 Panel）
  - 分别点评中国市场和美国市场表现
  - 提供趋势分析和投资建议
- **缓存机制**：每日仅生成一次，后续访问直接读取缓存

### 6. 股票搜索界面

添加自选股时的搜索功能：
- **模糊搜索**：支持输入股票名称或代码进行搜索
  - 例如：输入 "Apple" 可找到 AAPL
  - 例如：输入 "万达" 可找到相关股票
  - 例如：输入 "000001" 可找到对应股票
- **搜索结果表格**：显示序号、代码、名称、市场
- **直接输入模式**：按 `D` 键可直接输入股票代码和市场标识

### 7. 主要实验结果

*   **性能测试**：
    *   首次加载 A 股市场列表（约 5000+ 只股票）：约 30-60 秒（取决于网络速度）
    *   首次加载美股市场列表（500 只 S&P 500 成分股）：约 10-20 秒
    *   后续访问缓存数据：< 1 秒
    *   批量获取 20 只自选股价格：< 2 秒（从缓存）或 5-10 秒（从网络）
*   **功能完整性**：
    *   所有核心功能均已实现并测试通过
    *   支持 A 股和美股两大市场
    *   AI 分析功能在配置 API Key 后正常工作
    *   数据持久化功能稳定可靠
*   **用户体验**：
    *   界面美观，色彩丰富，信息层次清晰
    *   操作简单直观，支持快捷键导航
    *   错误处理完善，提供友好的提示信息
    *   加载状态显示，避免用户等待焦虑

*(注：实际运行效果可见于程序运行截图)*

## 五、项目总结

本项目通过整合 Python 生态中的优秀开源库（Rich, yfinance, Akshare, OpenAI），成功构建了一个轻量级但功能强大的终端股票追踪工具。

### 5.1 项目完成度

*   **核心功能完整**：实现了从数据获取、本地存储到界面展示、AI 分析的完整闭环
*   **多市场支持**：同时支持中国 A 股市场和美股市场，覆盖全球主要股票市场
*   **数据持久化**：使用 SQLite 数据库实现高效的数据缓存，大幅提升用户体验
*   **AI 集成**：成功集成 OpenAI 兼容接口，提供智能市场分析功能
*   **用户界面**：基于 Rich 库构建了美观、易用的终端界面

### 5.2 项目创新点

*   **智能缓存策略**：采用数据库缓存机制，每日仅需一次全量更新，后续访问实现秒级响应
*   **AI 增强分析**：将 Generative AI (LLM) 引入传统终端工具，提供了更智能的数据解读能力
    *   支持自选股个性化分析
    *   支持整体市场宏观分析
    *   自动生成专业的中文市场报告
*   **多源数据融合**：整合 Akshare（A股）和 Yahoo Finance（美股）多个数据源，实现统一的数据接口
*   **并发性能优化**：使用多线程并发请求，提高批量数据获取效率

### 5.3 技术亮点

*   **模块化设计**：清晰的代码结构，职责分离，便于维护和扩展
*   **错误处理完善**：全面的异常捕获和友好的错误提示
*   **性能优化**：数据库缓存、并发请求、批量操作等多重优化策略
*   **用户体验**：彩色界面、分页浏览、快捷键操作、加载状态提示等

### 5.4 项目扩展性

模块化的代码结构使得未来增加新功能变得十分容易：

*   **数据源扩展**：可以轻松添加港股、加密货币等新的数据源
*   **分析功能扩展**：可以添加技术指标分析、财务数据分析等功能
*   **界面扩展**：可以添加图表展示、数据导出等功能
*   **AI 功能扩展**：可以添加更多 AI 分析维度，如风险评估、投资建议等

### 5.5 项目价值

*   **实用价值**：为投资者提供了一个便捷、高效的股票行情追踪工具
*   **学习价值**：展示了 Python 在数据处理、API 集成、数据库操作等方面的应用
*   **技术价值**：探索了 AI 在金融数据分析场景下的应用潜力
*   **工程价值**：展示了模块化设计、性能优化、用户体验设计等软件工程实践

通过本项目的开发，充分验证了 Python 在快速构建实用工具方面的优势，同时也探索了 AI Agent 在金融数据分析场景下的应用潜力。项目不仅实现了预期的功能目标，还在性能优化和用户体验方面做了深入思考和实践。